# Pi Voice responsiveness, dictation UI, and behavior audit

## Scope and order

1. **Fix long-conversation UI lag first.** Trace Pi Voice's event-loop work during streaming, idle, rendering, playback and preprocessing. Reproduce/measure avoidable overhead with long synthetic histories, fix root causes, and add regression checks. Separate extension overhead from upstream Pi costs; do not claim the live UI is fixed solely from unit tests.
2. **Show dictation alternatives in the editor.** Trace the exact alternation syntax sent to the editing LLM and reuse it in live dictation UI rather than inventing another representation. Preserve editable drafts, cancellation fencing and review/submit behavior.
3. **Audit scrolling controls and shortcuts.** Follow button and keyboard paths through playback, pause, completion, replay, pending timing, ownership handoff and tail-follow. Specifically reproduce F10 after message completion, unreachable states and surprising no-ops. Report additional behavior changes for approval rather than silently implementing them.
4. **Audit pausing during timing generation.** Check late timing results, pause/resume intent, seek anchors and duration changes. Reproduce race conditions with deterministic tests and report findings for approval.
5. **Audit asset caching and invalidation.** Trace model and thinking-level changes across descriptions, narration, timing and audio caches. Implement the requested policy: changing the editing/model selection alone must not regenerate existing derived assets. Preserve invalidation for actual content, voice/audio or format incompatibilities. Document thinking-level behavior and any remaining issues.
6. **Report all findings.** Maintain `FINDINGS.md` incrementally as a compaction-safe investigation ledger with severity, reproduction/evidence, cause, status and proposed resolution. Distinguish fixed requested work, confirmed bugs awaiting a decision, and unverified hypotheses. Include additional UI/non-UI findings found along these paths.

## Follow-up phase: whole-sentence TTS, measured concurrency, and sentence navigation

Do this **after** the responsiveness/dictation/cache implementation and current behavior audit above.

7. **Generate whole sentences or real newline-delimited units.** TTS must receive complete sentences at once, never arbitrary streamed fragments. A literal source `\n` is also a boundary; terminal soft wrapping is not. Trace Markdown, code descriptions, streaming completion and final unterminated text. Preserve source mapping and code-narration cues. Add sentence/newline boundary regressions, including sentences arriving across multiple deltas.
8. **Benchmark parallel sentence inference before selecting concurrency.** Compare warmed sequential generation with 2 simultaneous sentences, then 3, 4, and upward while aggregate throughput improves. Use the same local model/voice/settings and non-private representative sentences; repeat runs and report medians. Measure aggregate audio-seconds generated per wall second **and time until the next ordered sentence is playable**. Keep latency at no more than **2× the equivalent sequential baseline**, and preserve playback order. Stop when throughput stops improving, latency exceeds the bound, or host resource limits intervene; choose the best admissible measured setting, not the highest worker count. Prefer 1 if no parallel setting qualifies. Include cold-start/memory caveats separately and retain responsiveness/cancellation while prefetching.
9. **Replace ±10-second navigation with sentence/newline navigation.** Forward/back controls and their shortcuts must move by actual sentence or literal `\n` boundaries, not ten seconds, words, or rendered line wraps. Define and test behavior mid-sentence, at exact boundaries, while paused, before timings exist, and at message ends; maintain source alignment for narrated descriptions. Update UI labels and Termux configuration to show only forward/backward symbols, removing `10`. Audit both full client and Termux copies and installation/docs; do not alter unrelated controls.
10. **Record results and outstanding decisions.** Add benchmark data, selected concurrency/latency tradeoff, navigation semantics, tests, and any newly found bugs to `FINDINGS.md`; update user-facing usage/shortcut documentation.

- [x] Whole-sentence/newline generation (`8c5a05b`); native long-sentence preservation checked. Long-unit alignment bounded in `a9b0b0c`.
- [x] Sequential vs parallel benchmark: initial standalone inference selected 4; the production-pool recheck selected **3** (1.76× throughput, 1.75× ordered latency). Four exceeded the 2× ceiling in the production recheck.
- [x] Ordered parallel playback with bounded lookahead, cache reuse, cancellation and child cleanup (`5e2b14c`). `PI_VOICE_TTS_WORKERS=1..8`, default 3.
- [x] Sentence/newline navigation, code-unit ordinals/cues, missing-timing support, pause retention and Termux symbol migration (`f003991`). Late completion ticks fenced in `b8ffd3a`.
- [x] Follow-up validation and review: typecheck and **169/169 tests pass**; production-pool offline benchmark recorded in `FINDINGS.md`.
- [ ] Deployment/live phone-and-SSH listening check; no active SSH sessions were interrupted.
- [ ] User decisions on unrelated audit findings below/in `FINDINGS.md`.

## Current discussion checkpoint

- **Single-action resume agreed (PLAY-5):** one explicit ⏯ resume must suffice even when recreating a transport or resolving a fallback/pending handoff. Do not inherit paused intent into a replacement requested specifically to resume. This is distinct from paused ↶/↷ navigation, which remains silent, and background regeneration, which never auto-resumes. Add cold/recreated transport regressions.

- **Stale playhead event fencing agreed (TIME-2):** ignore playback-position events from superseded requests before they can recompute/clear a newer preview marker, move its highlight or disturb its viewport. User suspects having seen this symptom too; keep that uncertainty. Cover stale ticks separately from stale completion/alignment events. Preserve current target during pending audio generation/acquisition.

- **Playback-status preservation agreed (UI-8):** unrelated setting changes (e.g. highlighting/autoscroll) must preserve actual playing/paused status, not mark a still-running transport idle. Keep this separate from current-asset invalidation, which immediately pauses with ownership retained and no auto-resume under the agreed rule.

- **Layout-aware manual re-anchor agreed (SCROLL-4):** invalidate cached absolute narration anchors on layout changes and recompute before explicit Alt+V/navigation framing, rather than using a stale five-second coordinate cache. Collapsing thinking/resize must not cause unsolicited paused viewport movement; resolve the current layout only when following or explicitly navigating as agreed. User reports frequently encountering this symptom. Do not equate this with a proven Ctrl+T key-handler failure or the separate Alt+T pause-scroll workaround.

- **Disabled-session attention lifecycle agreed (UX-1):** disabling Voice removes/suppresses that session's waiting registration, and routing skips disabled sessions so they cannot starve others. **Re-enabling must re-evaluate outstanding attention and requeue the project if still needed**, without creating duplicate entries, resurrecting already-handled requests, or reviving work explicitly cancelled by Stop. Respect queued/noninterrupting announcements and ownership. Exact queue priority on re-enable not specified; do not silently restore stale oldest timestamps in a way that starves other sessions.

- **Manual framing independent of autoscroll agreed (SCROLL-3):** disabling automatic scrolling suppresses unsolicited viewport motion, not explicit Alt+V or playback-navigation framing. Maintain target lookup/render markers for manual actions even with autoscroll off. Honor immediate target framing, paused state and current-asset source mapping.

- **Shutdown/reload handoff fencing approved (PLAY-2):** cancel pending replay/acquisition requests when their session shuts down or reloads, reject late completions, and prevent obsolete instances from reacquiring ownership or starting playback. Clean up a lease acquired by an obsolete request without releasing a newer owner's lease. User recognizes a similar prior bug; treat that as supporting feedback, not a fresh measured reproduction. Include this in the agreed batch.

- **Incomplete timing recovery agreed (TIME-1):** preserve valid partial timings after seeks and fill missing portions in bounded background work, including while paused. Do not play audio, move the paused highlight/viewport or release ownership to complete timing. Mark completeness only when the entire applicable narrated asset is covered, respecting current version/settings and resource limits. Avoid regenerating compatible completed parts or letting background results override newer navigation.

- **Transcript-order navigation clarified and agreed:** live speech, replay, message navigation **⏮/⏭**, and sentence/literal-newline navigation **↶/↷** must all use the same narration-eligible transcript ordered by its **actual transcript order**, filtered by current readability/narration settings. Include thinking traces when enabled; do not impose an artificial thinking→answer alternation or skip eligible traces. Sentence movement must work within and across eligible targets, skipping content excluded by current settings. Tie timing/highlighting to the exact eligible spoken content. Preserve established boundary, paused-navigation and immediate-framing behavior. Do not conflate thinking visibility with narration eligibility or invent a new ordering.

- **Description validation consistency approved:** generation acceptance and cache restoration must agree so an accepted description remains reusable after reload. Existing audit: summary mode accepted 1,649 characters while restore rejects >1,500; guided generation already limits total speech to 1,500. User recognizes a similar symptom and approves the fix; do not claim this proves the cause of their particular incident (current config is guided). No agreed new numeric limit: choose consistent validation with safe compatibility during implementation, not silent truncation.

- **Duplicate retry coalescing approved:** invalidate/retry once per unique **complete description-cache key**, sharing the result across matching occurrences. Do not deduplicate on code text alone: conversation-context differences remain separate keys; identical code usually shares a key only when all relevant identity inputs also match. User agreed after clarification.

- **Compaction-retained preprocessing fix approved:** user recognizes the described symptom and asks to fix it. Under since-compaction scope, include messages deliberately retained in active context across compaction, not merely entries created after the compaction marker. Schedule only missing descriptions/timings, reuse compatible cached work, and exclude messages actually summarized away. Add to the post-discussion batch; no new immediate implementation exception was requested. User recognition is additional feedback, not a measured reproduction of their exact session.

- **Local playhead/drain fix agreed:** keep playback-position updates and progress/highlighting alive while buffered local audio drains; mark completion only when playback actually ends, not when audio input finishes writing. User understands this is the audio playhead, not wall-clock time. Do not attribute Termux's reported symptoms to this host-local finding without evidence.

- **Sticky pause across messages agreed:** incoming assistant messages must not clear paused UI state or resume audio. Queue new speech while retaining ownership, the paused indicator and paused target until explicit resume or playback action. This does not override the established rule that navigation while paused stays paused, or Stop's cancellation/release semantics.

- **Paused alignment stability agreed:** retain improved timing results without moving the paused word/highlight or viewport; apply positional refinement upon explicit resume/navigation, not arrival of background alignment. Alt+V returns to the same frozen paused target. Preserve immediate explicit navigation and manual-scroll priorities; do not discard useful compatible timing data merely to freeze presentation.

### Immediate handoff — discussion completed; agreed implementation batch starting

- **ASR coverage/stability concrete acceptance example:** adding `acd` to an existing display `[a|b]c[b|d]` must leave it **exactly unchanged** because it is already represented. User explicitly clarifies that **a, b, c and d stand for arbitrary subexpressions**, not literal characters/words; there is no conflict with the word-boundary rule and no need to rewrite their example. Coverage checking must support nested subexpressions, not only a flat token Cartesian product. Do not add provenance markers or recompute a different equivalent layout for already-covered additions. Include this as a regression criterion when implementing.
- **Long-unit alignment agreed:** replace the blanket >30s forced-alignment skip with bounded overlapping-window alignment/refinement and merged timestamps, while keeping sentence playback/navigation atomic. Do not block playback for alignment; retain clearly indicated estimates wherever alignment is unavailable or unreliable. Bound resources in conjunction with the approved alignment queue work; preserve source mapping and prevent duplicated/out-of-order word timestamps at seams. Implementation belongs to the post-discussion batch.

- **ASR UX priority clarified further:** intuitive reading and limiting misleading accidental overlaps take priority over the mathematically smallest expression. Word-boundary-only factoring necessarily prevents some minimal character combinations. Extra combinations remain permitted, but should not be multiplied needlessly just to shorten the display; prefer clear phrase grouping/nesting or separate alternatives when an overlap would confuse. Preserve all actual candidates and leave LLM evidence untouched. This refines, not revokes, the previously agreed incidental-coverage/display-stability rule.

- **LATEST ASR policy clarification — overrides older constraints below:** user permits **nested alternatives** and explicitly permits the display grammar to generate combinations that were not actual ASR hypotheses. Every real hypothesis must be represented, but a candidate already expressible accidentally through other combinations must **not change the displayed output merely to mark that candidate explicit**. No branch provenance/linking is required for display. **Never split inside a word: factor only at word boundaries.** Maximize readable compact sharing subject to that restriction; keep full/multiline alternatives as needed for divergence. This is user-facing preview only; leave actual candidate arrays, candidate order/evidence and editing-model request unchanged. Earlier insistence on linked branches/no invented combinations, and permission to factor matching word fragments, are superseded. Exact formatter implementation and readability thresholds remain to be designed/tested in the later batch; do not imply a globally shortest grammar is guaranteed.

- Latest user correction: ASR preview should **always factor out shared text as compactly as possible**. Do not present `follow [my advice|the advice]` when `follow [my|the] advice` works. They asked for the entire UX proposal reformulated and durable handoff notes before compaction.
- Confirmed ASR scope: **user display only; leave editing-LLM candidate JSON/request unchanged**. Similar candidates should share common text outside alternatives. For divergent/long candidates, full alternatives on separate lines are acceptable; richer interaction is not requested. The formatting algorithm is still being discussed, not implemented.
- Pending design distinction: maximizing factoring across *multiple separated differences* can require linked choices to preserve actual candidates. Proposed refinement to discuss: factor common text throughout, but label/link branches if independently combining them would invent candidates; alternatively use lossless multiline candidates when linkage would be confusing. Do not silently treat all brackets as independent choices or claim a single prefix/suffix span always achieves maximum compactness. Preserve original candidate text/order and manual editor ownership. Layout stability is secondary to the user's explicit compactness preference.
- Overall workflow is **discuss all points one by one, then implement agreed work and return with follow-ups**. The remaining decisions were completed through PLAY-5 single-action resume approval; **begin the agreed implementation batch now**. Earlier 'do not implement yet' notes below describe the prior discussion phase and are superseded. Immediate exceptions already implemented: runtime TTS worker setting and consistent read-only setting queries/autocomplete cleanup. Future-only integrated narration and richer selection UI remain excluded. Preserve active sessions and personal settings; use independent commits, tests and review.
- Latest implementation HEAD: `09e7d18` (hide duplicate singular alias in autocomplete). Earlier immediate commits: `82384dd` runtime tts-workers; `7c90403` query/alias; `4e159c1` all-setting queries; `f23f5a1` effective shortcut query reporting. Full suite reported 179 passing at `f23f5a1`; `09e7d18` passed typecheck and focused integration regression. No live sessions or personal settings changed by these implementations; user reports reloading afterward. Last inspected worker setting before user changes was default 3 (no persisted/environment override); do not assume that remains their current setting without checking.
- `PLAN.md` and `FINDINGS.md` contain **uncommitted discussion notes**; preserve them and intentionally untracked `ISSUES.md`. No push/deployment or live session restart is authorized implicitly. User-level `voice-implementer` agent was created at `~/.pi/agent/agents/voice-implementer.md`; agent discovery is dynamic. `code-reviewer` remains read-only. LSP unavailable; use typecheck/tests. No paid application-provider calls, private transcript exports or real inference without authorization.
- Saved feedback: start/stop typing lag but no pause/resume lag; Escape visual cancellation lag reproduced by user with no audio playing (no measured keypress latency); intermittent speech interruption followed by “project pi requires attention next”; Termux-window-switch correlation not reproducible; paused manual scrolling sometimes blocked, Alt+T workaround, Alt+V unreliable; ⏯/↺ silent after ⏭ enters tail at latest message; native jump-to-latest banner spam/last-line occlusion. Do not invent causes. Three existing sessions were observed (`mu`, `humo-loan-scorer`, this `pi`); at inspection this session held speech ownership and no waiting/preemption records remained.
- Agreed batch behaviors are detailed below: Stop cancellation/suppression escape hatch; explicit playback finalizes captured dictation except Stop; acquisition second-tap cancellation; device pinning/current-attachment `/voice reconnect` and explicit playback repinning; ↺ always this project; queued noninterrupting attention; free paused scrolling and immediate target previews; start anchor at 20%, ongoing 20–80% window and agreed tail/manual priorities; dirty current assets pause immediately with ownership retained/no auto-resume; omission/budget fixes; full conversation context through next code-block opening/message end with wait accepted; exact spoken-asset timing identities/stale-result fencing; bounded prioritized alignment with indicated estimates on overload.
- Future only, explicitly NOT today's work: main-conversation model generates integrated inline descriptions/highlighting instead of side-context narration, with eventual code-block presentation changes. Also defer richer code-description regeneration selection UI; existing code-retry commands/basic picker remain for now.


- **ASR display scope clarified:** compact alternatives are **user-facing only**; leave the editing LLM's working candidate JSON/request unchanged. User suggests full-sentence alternatives for divergent candidates, possibly on separate lines, and asks for UX/reliability recommendations. Proposed safe default (not yet agreed): show common word-aligned prefix/suffix once with a single alternative span between them; use multiline full candidates when compact formatting would be unwieldy. One linked span preserves candidate relationships instead of implying unsupported cross-combinations across multiple independent brackets. Preserve full candidate text/order; identical candidates may be collapsed for display only. Treat formatting as a temporary preview, not model confidence or an automatic final prompt. Do not implement until display policy is agreed.

- **Alignment backlog recommendation agreed:** bound queued alignment work and prioritize current/upcoming speech. When alignment falls behind, keep playback responsive with clearly indicated estimated word timings rather than waiting for alignment and creating audio gaps. Do not claim estimates are forced alignment or promise later refinement without retained/recoverable audio. Design resource limits and cancellation during implementation.
- **ASR display feedback/question:** user now sees ASR alternatives/resolution evidence but finds tagged JSON cumbersome; prefers compact inline alternatives such as `fo[r row|ollow] your recommendat[ion|ed ion]`. Discuss feasibility/reliability: close candidates can be aligned into differences, divergent hypotheses need larger phrase/whole-candidate alternatives, and independent brackets can imply unobserved combinations. Preserve actual candidate arrays/evidence for the editing model; any compact view must be distinguished from raw evidence and preserve draft ownership. No display policy agreed yet; user is asking what is possible.

- **Timing identity analysis accepted:** compatible timing must identify actual spoken content/audio version, not merely a code block or its stable description lookup key. Regenerated wording requires new timing even with unchanged source code; preserve compatible versions for switching back. This joins the agreed stale-result fencing work.

- **Timing-version fencing agreed:** associate jobs/results with the exact asset version they started against; ignore stale results after relevant input changes, keep compatible historical timing for switching back, and never resume playback from late results. User asks whether timing identity is tied to a sound file or a code block. Current implementation stores message-level timing snapshots by message ID/render key, while sentence Opus files have separate text/TTS-settings hashes; no waveform checksum binding currently. Ensure regenerated description content changes invalidate timing even when the source-description lookup key stays the same (current dependencies encode description key plus ready/omitted/missing, not the plan's content).

- **Authorized immediate command-query follow-ups completed:** `7c90403` adds read-only no-argument `/voice tts-workers` and singular `tts-worker` alias. `4e159c1` extends value-without-argument queries consistently across `/voice` setters, reporting effective settings before any playback/tail/budget mutations. Actual action commands retain their action semantics. `/voice voice` now reports rather than opening its old picker; completions remain available. Review found an effective-shortcut reporting issue for overwritten bindings; `f23f5a1` fixes reporting (not collision policy) with Alt+T/F5 regressions. Implementer reports typecheck and **179 tests passed**. No personal settings, live sessions, or other pending fixes changed. User prefers this query behavior generally.

- **Explicit immediate exception:** user requests a runtime TTS concurrency setting/command, like other `/voice` settings, implemented now via a subagent. Intended scope: validated 1–8 workers (default 3), runtime propagation, safe resizing/cleanup, no unnecessary asset invalidation or audio restart; document environment/config precedence and add tests. Other discussed fixes remain deferred. Initial delegation failed because only a read-only reviewer was configured; that was not a hard limit. Created user-level `voice-implementer` agent (definitions are discovered per call) and delegated the implementation. **Completed in `82384dd`**, reviewed by `code-reviewer` with no actionable introduced findings. `/voice tts-workers <1..8>` persists and updates a running worker without restarting audio or invalidating assets. Persisted valid setting overrides legacy environment fallback, then default 3. Excess busy workers finish ordered jobs before retiring on shrink. Subagent reports typecheck and 172 tests passed. No real inference/provider calls or live-session restarts; user must load the updated extension before using the command. All other discussion items remain deferred.

- **Context scope and wait confirmed:** use the full conversation-model context through the next code-block opening or the current message end, plus narrator/explanation instructions and the existing code-highlighting protocol. User explicitly accepts waiting for that boundary during streaming for now. Preserve applicable system/tool/message context, rather than substituting a prose-only summary; investigate exact provider-context reconstruction/limits before claiming byte-identical requests. Do not include later blocks/content. This supersedes narrower block-only/through-fence proposals for the requested narration behavior; clarify treatment of configurable block-only mode during implementation if necessary.
- User suspects parallel TTS contributes to freezes and asks how to adjust it. This is a hypothesis, not a diagnosis; Escape lag was also observed without audible playback (background inference was not ruled out). Existing host-side `PI_VOICE_TTS_WORKERS=1..8` controls synthesis concurrency/lookahead, default 3, and is read when the worker starts. Explain how to launch Pi with a lower value, without changing settings or restarting sessions on their behalf. Timing/code-description preprocessing concurrency is separately configured.
- **Future TODO — explicitly NOT today's work:** integrate code narration/highlighting into the main conversational model context/output, replacing the separate narrator side context. User envisions replacing separately displayed/narrated code blocks with inline descriptions and model-authored highlighting in subsequent prose where needed, reducing repetition at generation time. Preserve this as future design work; exact code visibility/presentation and highlighting protocol need discussion before implementation. Do not remove code blocks or alter the main model's output protocol in the current round.

- **User-selected code-description context boundary (supersedes through-block proposal):** include context up to the end of the containing assistant message OR the start of the next code block, whichever comes first. Include prose following the target block, but not the next block. Prompt the model to explain the target/latest included code block in that relevant context, avoiding redundant explanations already present in surrounding prose. Apply the same canonical boundary to generation/cache lookup across live, replay and backfill.
- Consequence to discuss: during streaming, the following prose/context is not final until the next code-block opening or message end arrives. Stable generation may need to wait for that boundary; do not silently generate twice or key against different context later. Proposed redundancy policy: code narration complements surrounding prose (focus on code-specific details), while original prose remains narrated; user has not authorized automatically deleting/skipping original prose or guaranteed semantic deduplication. Clarify timing tradeoff before implementation.

- Retry/omission discussion accepted: keep existing retry commands/basic picker for this round, defer richer regeneration selection. Preserve omissions until an explicit retry or genuinely changed description inputs create new work; unrelated settings must not restart failed requests, and retries remain budgeted.
- **Budget fix agreed:** historical preprocessing budget must not replenish on ordinary sweeps/replays or unrelated settings changes; session-only allowance survives those changes. Replenishment only on a new session or explicit `/voice code-budget` authorization. Preflight failures that never call the provider do not consume request budget. User approved after explanation of sweep triggers. Implement in the agreed post-discussion round, not as an exception to discuss-all-first.

- Retry discussion: user wants command-only controls for now; defer a richer way to choose specific descriptions for regeneration. Existing commands are `/voice code-retry current` and `/voice code-retry historical [all|<message-id>]` (historical without an argument already opens a basic failed-description picker). These retry recoverable omissions/failures, not arbitrary successful descriptions. Explain this limitation; do not treat the question as authorization to build a new selection UI or claim force-regeneration exists.

- **Current-asset invalidation confirmed:** voice, TTS model, tone, text, or any other change requiring regeneration of the currently playing asset must **pause immediately and retain ownership**. Dirty/pending regeneration counts immediately, not just when replacement data arrives. **No automatic restart/resume** on regeneration completion or late callbacks; playback resumes only on an explicit user action. Tone is an example of an audio-affecting input, not a request to add a new tone setting. Do not invalidate compatible assets for unrelated editing-LLM changes.

- **Clarified invalidation rule (not a shortcut change):** pause and retain ownership whenever a change affects the **currently playing text/audio asset**, including when it becomes dirty and is expected to change soon. Apply this by actual asset dependency, not a fixed list of settings. Changes unrelated to the current asset must not trigger this pause. The user was not requesting F-key behavior changes; the assistant's suggestion to restart at the current sentence is **not agreed**. Keep the asset invalidation rule separate from any future resume-position discussion.

- **Agreed settings/ownership behavior:** microphone-only changes must retain speech ownership while existing playback continues. If a setting/action changes the narration currently being played (e.g. changing the TTS voice or regenerating the code block being spoken), apply the change and **pause while retaining ownership**. Other sessions may queue attention but cannot take over. Do not let stale old-setting audio/results become the resumed narration. Resume behavior/source anchoring and output-device changes may need specific clarification; do not assume the user approved releasing ownership for these narration updates.

- **Stop exception clarified:** user wants `/voice stop` as an escape hatch for a hung operation. Keep its cancellation semantics: stop/cancel recording and pending transcription/editing without waiting for final transcription, and prevent late results from applying. Do not auto-finalize dictation as a prerequisite for Stop. Preserve existing/manual draft content. This is the exception to the proposed capture-finalization flow for ordinary explicit playback actions.

- **New dictation-interruption preference:** user wants voice-related user actions (e.g. playback buttons) to stop recording and transcribe audio captured so far, rather than refuse playback or discard the recording. Explain the existing `/voice test` overlap scenario (command invoked while recording) and recommend a shared stop-capture → retain/finalize captured audio → requested action flow, with draft ownership preserved and no automatic submission caused by playback. Clarify scope for read-only status/settings queries and especially `/voice stop`, whose earlier agreed semantics cancelled dictation; do not silently resolve that conflict. Second 🎙 during pending acquisition still cancels startup; there may be no audio yet.

- Discuss remaining items **one at a time**, not bundled groups. User acknowledged/agreed to the microphone-start cancellation fix: a second 🎙 while microphone/device ownership setup is pending cancels that pending start, so recording cannot begin afterward. This concerns microphone acquisition, **not SSH reconnection**. The bug was previously audit-reproduced (UX-4), not newly experienced by the user. Next discuss playback tests versus active dictation, then settings changes versus speech ownership separately.

- **New live scrolling/UI report:** while voice is on and re-anchoring near the final lines, “↓ Jump to latest message” repeatedly appears and obscures the last line. Investigate Pi's existing jump-to-latest/tail-follow integration rather than layering a competing hint. Treat the visible spam/occlusion as user-reported; cause is unconfirmed.
- **Requested navigation behavior:** playback navigation shortcuts while paused must immediately reveal/anchor the destination text, before audio generation/startup, without unpausing. Include code-description sentence destinations and ownership-wait states in the next analysis.
- User wants to try to fix **all recorded points** in the next implementation round, after discussing defaults/semantics. This does not lift the current discuss-first restriction.
- **Scrolling clarifications overriding the earlier proposal:** user thinks “↓ Jump to latest message” is Pi's native banner, not ours; verify its origin and interaction with Voice before choosing a fix. Do not assume we implemented a duplicate banner.
- **Required start framing:** on audio/playback start, place the narrated target at the **top of the 20–80% window (20% down the viewport)** even if it is already visible/in-band. Clamp at transcript boundaries; do not create blank space past the end. Explicit play must move/frame immediately, before audio generation/ownership acquisition completes; not only paused navigation. Avoid a redundant second jump when audio becomes ready if already correctly framed, and respect subsequent manual scrolling.
- **Remaining scroll defaults agreed by the user (with the start-framing corrections above):** manual scrolling overrides automatic motion until an explicit follow/navigation action; explicit jump-to-end pins immediately; new text extending a pinned end during ongoing narration restores 20–80% windowed following, moving only as needed during continuation, while remembering final return-to-tail intent; final completion follows tail only if still intended and not overridden by manual browsing; queued next-message playback frames that message rather than bouncing through tail. Paused navigation previews immediately without resuming. The previous proposal's “keep still if already in-band” exception must not apply to playback starts.

- **Agreed attention/replay semantics:** never interrupt current speech with another project's attention announcement; queue the announcement for the next safe opportunity after current playback finishes. **↺ always replays this project**, never switches to another waiting project; this supersedes the previously open ↺ priority question. Explicit replay also follows the agreed current-connection repinning rule.
- Stop clarification: proposed “suppresses announcements and retries” means **suppresses both**, not pause-then-automatically-retry. No delayed restart of previously stopped work. An explicit playback action or genuinely new response may authorize fresh work, but must not resurrect cancelled replay/test requests. User asked this distinction; explain it directly.

- **Agreed device routing:** pin the device from which the session connected; do not silently fall back to another client or host speaker/microphone when a pinned device becomes unavailable. Stop the affected operation, notify, and require an explicit retry after reconnection.
- **Agreed explicit-playback routing:** user-triggered replay/playback actions, including playback shortcut keys, also adopt and pin the current connection's device before starting playback. Resolve fresh attachment identity as for `/voice reconnect`; never silently choose another device if it is unavailable or ambiguous. Automatic speech and attention retries must not repin merely because a different client became active. Preserve pause intent during navigation; pause-only actions need not repin.
- **Requested command: `/voice reconnect`** (user corrected `/reconnect`). Resolve the current connection's device ID and adopt it as this session's new pinned device. Implement after discussion completes. Must account for a long-lived Pi/tmux process retaining an old environment; do not assume startup `process.env.PI_VOICE_DEVICE_ID` always identifies the newly attached connection. If a current device cannot be identified unambiguously, report it rather than guessing/falling back.
- Current settings inspected read-only: global input/output are both `auto`; this process inherits `PI_VOICE_DEVICE_ID=1d845b64-58f6-444a-91a8-368adbe906d4`, registered as Termux (`localhost`). Existing auto routing only prefers that ID, then falls back to another connected device; it is not yet a strict pin. No configuration changed.

- User agrees with the paused-scrolling point: allow free manual scrolling while paused and make Alt+V return to the paused narrated position without resuming audio or trapping subsequent manual scrolling.
- New report: **Escape is slow to stop visually** in the current roughly **500-message** conversation; already-started audio continues. Pi overhead or an audio-related cause remains unconfirmed. User authorized a harmless loop-script cancellation test: run periodic output and let them press Escape. This is a diagnostic exception to discussion-only mode, not authorization to implement fixes. Test completed: the second loop ran without preceding assistant commentary, and the user confirms visual cancellation lag **still occurs with no audio playing**. Active playback is therefore not required; Pi core versus extension/background/rendering/transport causes remain unisolated. No keypress latency was measured. For further silent cancellation tests, invoke the tool directly without a spoken preamble.

- Paused-scroll clarification: user believes the workaround was **Alt+T**, not Ctrl+T. Alt+V sometimes seems to do nothing or prevent further scrolling; they are not certain which. Preserve that uncertainty, and do not attribute this report to thinking visibility.
- Corrected end-of-message report: after **⏭** at the latest message enters tail-follow, neither **⏯** nor **↺** starts audible playback. The earlier report was not a request to make ⏭ or Alt+V replay. Desired behavior from the discussion: pressing a playback control from that end/tail state should replay the last message and resume tail-following after it finishes. Confirm how ↺ should interact with another project's pending attention. Preserve ordinary paused ↶/↷ navigation without resuming audio.

- **Agreed Stop semantics:** `/voice stop` stops speech and dictation, cancels pending replay/test/ownership requests, and suppresses attention announcements/retries from restarting audio. Voice remains enabled; an explicit playback action or genuinely new response may speak again. ⏯ pause instead retains the current audio and ownership for resume. Implement after the discussion of all points is complete.
- **New paused-scrolling feedback:** sometimes manual scrolling is blocked while paused; user reports toggling what they call “Ctrl+T tail/untail” restores scrolling. Paused playback must allow free manual scrolling. They also report Alt+V behaving incorrectly while paused and ask its purpose. Confirm the actual key/function rather than assuming Ctrl+T is tail-follow (earlier audit found Pi binds Ctrl+T to thinking visibility; Pi Voice's default tail shortcut is Alt+T). No cause established.

- User has reloaded. Discuss **all remaining points first**, one by one; only then implement the agreed changes and return with follow-ups if needed. This supersedes the earlier autonomous-continuation instruction below. Keep durable notes; do not start fixes during this discussion.
- Use Termux **icons directly in instructions**, not a separate Fn-key legend: ↺ replay/attention, ↶ previous sentence, ↷ next sentence, ⏯ pause/resume, ⏮/⏭ previous/next message, 🎙 dictation. Stop is the typed `/voice stop` command; no Stop icon exists in this row.
- Current point: live validation after reload. Combine the check instructions with those icons in one message. **User has returned; iterate through all feedback and remaining points before implementation.**
- New user report: **huge typing lag specifically when voice starts or stops playing; the rest is a lot smoother**. Event-loop saturation/synchronous I/O are user hypotheses, not established causes. Record and investigate transition-specific latency after discussion/agreement; earlier synthetic performance tests do not rule out this live problem.
- Latest live report: narration began, then unexpectedly stopped/crashed from the user's perspective and announced **“project pi requires attention next”**. Cause unknown; no process crash has been confirmed. User was in another Termux window and returned around the incident, but cannot tell whether it occurred before or after returning. A subsequent repeat of the window switch did not reproduce the interruption. Treat it as a possible correlation, not an established trigger; the original incident remains unresolved.
- Read-only coordinator/process check confirmed three existing live sessions (`mu`, `humo-loan-scorer`, this `pi` session), all predating the incident; user has started no additional session since this setup. At inspection, this `pi` instance held speech ownership and current attention, with no waiting/preemption entries. This does not establish the cause. No sessions or transports were modified.
- User returned and confirmed: typing lag occurs **only on playback start/stop, not on ⏯ pause/resume**. Cause remains undiagnosed. Discussion has resumed; still discuss all points before implementing.
- Open validation questions: whether ↺ replay, ↶/↷ sentence navigation, paused navigation and `/voice stop` behave as intended. Do not record these checks as passed.

## Updated instructions after reload

- User confirmed reload after performance commit `f504597`; continue autonomously with the remaining implementation and audit.
- Expand the UX audit to annoying no-ops, inconsistent toggles, user-stuck states, and interactions between multiple Pi sessions (focus, ownership, pending acquisition, pause/resume, shutdown/reload).
- Investigate the exact **Ctrl+T while paused** report; distinguish Pi's own keybinding from Alt+T and configured Pi Voice shortcuts.
- Keep `FINDINGS.md` updated after each useful result, including reproduction steps, what is fixed, what remains proposed, and where to resume after compaction.
- Finish live alternatives using the actual LLM evidence syntax and model-independent cache reuse. Report unrelated behavioral fixes for a decision rather than silently applying all audit findings.

## Guardrails

- Keep the R2T2 experiment off main; preserve untracked `ISSUES.md` and unrelated work.
- No paid provider calls or private transcript exports. Prefer synthetic histories and mocked inference.
- Reuse current render/cache/control mechanisms and existing tests; no speculative infrastructure.
- Add runnable regressions for fixes; run typecheck, relevant tests and the full suite.
- Use separate atomic commits for independent fixes. Update this plan and findings as evidence emerges.

## Progress

- [x] Record scope before investigation.
- [x] Measure and fix avoidable extension event-loop overhead (149 tests pass; live session must reload to use changes).
- [x] Implement live editor alternation notation, cancellation/draft safety, and actual worker candidate routing.
- [x] Audit scrolling/buttons/shortcuts; actual idle-completion F10 test passes; remaining findings await decisions.
- [x] Audit pause/timing-generation races; unresolved control/ownership defects recorded.
- [x] Implement model-independent reuse and audit remaining invalidation.
- [x] Complete initial regression/full-suite validation and report findings (160 tests, including whole-sentence safety and real worker control-path regressions).
